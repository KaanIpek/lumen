// Capacitor resolves the native class through the podspec and the ObjC bridge;
// this file only has to exist so the package is loadable. js/iap.js reaches the
// plugin with Capacitor.registerPlugin('LumenStore').
module.exports = {};
