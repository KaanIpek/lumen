// Deliberately empty of logic, like the other three native halves.
//
// The game reaches this through Capacitor.Plugins.LumenAuth in js/auth.js;
// there is no bundler here, so an import would never run. The package exists
// for its NATIVE half.
module.exports = {};
